schema_version: "1.0"

extension:
  id: "jira"
  name: "Jira Integration"
  version: "1.4.2"
  description: "Convert spec tasks into Jira tickets with plan/apply jira-map.md, incremental sync, and configurable progress recording"
  author: "Patrick Fowler"
  repository: "https://github.com/pdfowler/spec-kit-jira"
  homepage: "https://github.com/pdfowler/spec-kit-jira"
  license: "MIT"

requires:
  speckit_version: ">=0.8.3"
  tools:
    - name: "Atlassian MCP"
      required: true
      description: "Atlassian Remote MCP Server — provides Jira Cloud read/write access"

provides:
  commands:
    - name: "speckit.jira.taskstotickets"
      file: "commands/taskstotickets.md"
      description: "Plan or apply Jira tickets from tasks.md via plan/apply subcommands and flat jira-map.md"

    - name: "speckit.jira.implement"
      file: "commands/implement.md"
      description: "Run implementation scoped to a phase ticket; records Jira progress using configurable gates"

hooks:
  after_tasks:
    command: "speckit.jira.taskstotickets"
    optional: true
    prompt: "Plan Jira tickets from tasks? (runs taskstotickets plan — apply separately after reviewing jira-map.md)"

tags:
  - "jira"
  - "atlassian"
  - "project-management"
  - "issue-tracking"
