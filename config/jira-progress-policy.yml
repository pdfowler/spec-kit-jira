# Deprecated in spec-kit-jira 1.5.0.
#
# Default configuration now lives in:
#
#   extension.yml -> config.defaults
#
# The installable template now lives at:
#
#   config/jira-config.template.yml
#
# Projects should copy that template to:
#
#   .specify/extensions/jira/jira-config.yml
#
# Existing project-level `.specify/jira-progress-policy.yml` files remain readable
# for one compatibility window and are merged under `jira.progress_policy`.

schema_version: "1.0"
deprecated: true
replacement: "config/jira-config.template.yml"
